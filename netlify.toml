[build]
  command = "npm run build:netlify"
  publish = "build/client"

[dev]
  command = "npm run dev"
  port = 5173

# Handle client-side routing fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
